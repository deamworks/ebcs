# CLAUDE.md

บริบทโปรเจกต์สำหรับ Claude/agent ที่เข้ามาทำงานต่อในโปรเจกต์นี้ — repo: `deamworks/nbtc-filing`

**สารบัญ**: ภาพรวม · สรุปส่งมอบระบบ (อ่านก่อนถ้ามีเวลาแค่หัวข้อเดียว) · ธรรมเนียมโปรเจกต์ ·
จุดที่ควรระวังเป็นพิเศษ (บั๊กที่เคยเกิด) · ประวัติการเปลี่ยนแปลง PR #1–185 · สถานะปัจจุบัน · Known Issues

## ภาพรวม

ระบบยื่นแบบและนำส่งเงินรายได้ประจำปีต่อ กสทช. (NBTC Filing) ดู [README.md](./README.md) สำหรับ tech stack และวิธีรัน

## สรุปส่งมอบระบบ — ถึงผู้ดูแลระบบคนต่อไป

ระบบนี้พัฒนาขึ้นเพื่อรองรับการยื่นแบบและนำส่งเงินรายได้ประจำปีของผู้ประกอบกิจการกระจายเสียง/โทรทัศน์
ต่อ กสทช. พร้อมใช้งานจริงแล้ว ผ่านการทดสอบและแก้ไขบั๊กมาต่อเนื่องกว่า 200 รอบการเปลี่ยนแปลง
(ดูรายละเอียดทั้งหมดในหัวข้อ "ประวัติการเปลี่ยนแปลง" ด้านล่าง)

**สถาปัตยกรรมที่เลือกใช้**: Flask + MySQL + vanilla JS เลือกเพราะระบบนี้เป็นฟอร์มกรอกข้อมูล
หลายขั้นตอนที่ต้องแก้ไขบ่อยและรวดเร็ว การไม่มี build step ทำให้แก้ไฟล์แล้วรีเฟรชเห็นผลทันที
เหมาะกับทีมขนาดเล็กที่ต้องดูแลต่อเนื่อง รันผ่าน Docker Compose ชุดเดียวทั้ง dev/production
เพื่อให้ deploy ง่าย ไม่ต้องมี infrastructure ซับซ้อน — แลกมาด้วยข้อจำกัดว่าไม่มี auto-scaling
หรือ high-availability ในตัว ถ้าระบบโตขึ้นมากในอนาคตต้องพิจารณาแยก service ออกจากกันจริงจัง

**กลไกหลักที่ต้องเข้าใจก่อนแตะโค้ด**: ระบบยึดสถานะใบยื่นแบบเป็นแกนกลาง
`draft → pending_payment → paid` เดินหน้าทางเดียว ล็อกไม่ให้แก้ไขข้อมูลได้อีกเมื่อพ้น draft แล้ว
ทั้งสองฝั่ง (ผู้ประกอบการและแอดมิน) — เคยลองเปิดให้แอดมินแก้ไขได้ (PR #138) แล้วพบว่าสร้างปัญหา
เรื่องความน่าเชื่อถือของข้อมูลที่ยื่นไปแล้ว จึงตัดสินใจถอดออกทั้งหมดและคงไว้แค่ "ตีกลับเป็นร่าง" แทน
เป็นการตัดสินใจที่ผ่านการลองผิดลองถูกมาแล้วจริง ไม่ใช่ข้อจำกัดทางเทคนิค — ไม่แนะนำให้เปิดกลับมา
โดยไม่คุยกับทีมนโยบายก่อน (ดูรายละเอียดเพิ่มในหัวข้อ "จุดที่ควรระวังเป็นพิเศษ" ด้านล่าง)

**สิ่งที่ยังไม่เสร็จ — พูดตรงๆ ไม่ปิดบัง**: ส่วนที่ควบคุมได้เองทำครบแล้ว แต่มี 3 เรื่องที่ต้องรอ
ปัจจัยภายนอก: การเชื่อมต่อ Data Center/SAP ยังเป็น mock (รอ API spec จริงจาก กสทช.), QR Code
เป็นมาตรฐานถูกต้องแล้วแต่จ่ายเงินจริงยังไม่ได้เพราะ Biller ID ยังไม่ยืนยันกับธนาคาร, และผู้ประกอบการ
ยังเปลี่ยนอีเมลตัวเองไม่ได้ต้องให้แอดมินแก้ให้ (รายละเอียดในหัวข้อ "Known Issues" ด้านล่าง) นอกจากนี้
ระบบไม่มี automated test — ทุกการแก้ไขต้องทดสอบด้วยมือให้ละเอียดก่อน merge จริง

**ความมั่นใจในสถานะปัจจุบัน**: ส่วนที่อยู่ในการควบคุม — flow การกรอกแบบ, การคำนวณเงิน,
การจัดการสิทธิ์ผู้ใช้, ระบบเอกสารแนบ, audit log — ผ่านการ iterate และแก้บั๊กจริงมาหลายรอบจนนิ่งแล้ว
ใช้งานจริงได้โดยไม่มีบั๊กที่รู้แล้วยังไม่แก้ค้างอยู่ สิ่งที่ยังไม่เสร็จทั้งหมดเป็นเรื่องที่ต้องรอคนอื่น
ไม่ใช่งานที่ทำไม่จบ

## ธรรมเนียมของโปรเจกต์นี้

- **Commit message และ PR title/body เป็นภาษาไทย** เกือบทั้งหมด — ให้ทำตามธรรมเนียมเดิม
- **คอมเมนต์ในโค้ดเป็นภาษาไทย** เน้นอธิบายว่าโค้ดตรงนั้นทำงานอย่างไร/เพื่ออะไร ให้คนอ่านเข้าใจโค้ดได้ทันที ไม่ใช่การจดบันทึกว่าเคยแก้ไขอะไรไป
- **ไม่มี automated test suite** (เอาออกใน PR #116 หลังเคยเพิ่มใน PR #113) — ต้องทดสอบด้วยมือ/อ่านโค้ดให้ละเอียดก่อนส่ง
- **Frontend เป็น vanilla JS ไม่มี build step** — แก้ไฟล์ใน `frontend/js`, `frontend/pages` แล้วรีเฟรชดูได้เลย ไม่ต้อง compile
- **แต่ละ PR ควรมีขอบเขตแคบ ทำเรื่องเดียว** — ดูจากประวัติ commit ที่ผ่านมาแทบทุกอันแก้ปัญหาเดียว/ฟีเจอร์เดียว
- **แต่ละ session ทำงานบน branch `claude/<slug>` แยกจาก main แล้วเปิด PR** — ดู pattern ชื่อ branch จาก PR ที่ผ่านมา

## จุดที่ควรระวังเป็นพิเศษ (บทเรียนจากบั๊กที่เคยเกิด)

1. **การจับคู่เอกสารแนบ (`.doc-row-name` ↔ `doc_type`)** — โค้ดหลายจุด (`frontend/js/view-submission.js` `_vsRenderAttachments`, `frontend/js/print.js` `handleFileAttach`) ใช้ข้อความเต็มของ `.doc-row-name` เทียบ/บันทึกเป็น `doc_type` ตรงๆ แบบ string เปะๆ ถ้าจะแก้ label ที่แสดงผล (เช่นเพิ่มดอกจัน, ไอคอน) **ต้องเช็คว่ามีจุดไหนใช้ text นั้นไปจับคู่ข้อมูลจริงด้วยหรือไม่** ไม่งั้นเอกสารที่แนบไว้แล้วจะดูเหมือนหายไป (เคยเกิดจริงใน PR #183 → ต้องแก้เพิ่มใน PR #184)
2. **แบนเนอร์สถานะในโหมดดูอย่างเดียว** (`_vsRenderStatusBanner` ใน `view-submission.js`) แชร์ระหว่างหน้าผู้ประกอบการ (`index.html` เรียกผ่าน `viewExistingSubmission()` ใน `index.js`) และหน้าแอดมิน (`admin-view-submission.html`) — แก้ปุ่ม/ข้อความในนี้กระทบทั้งสองฝั่ง ต้องเช็คทั้งคู่
3. **รายการเอกสารแนบ (`#doc-upload-list`) เป็น static HTML ที่ซ้ำกันสองไฟล์**: `frontend/pages/index.html` และ `frontend/pages/admin-view-submission.html` — แก้ต้องแก้คู่กันทั้งสองไฟล์เสมอ (ไม่ได้ share component)
4. **สถานะใบยื่นแบบที่ไม่ใช่ `draft` แก้ไขข้อมูลไม่ได้อีก** ทั้งฝั่งผู้ประกอบการและแอดมิน (PR #150, #168) — ถ้าจะเพิ่มปุ่ม/action ใหม่ในหน้าดูใบยื่นแบบ ต้องเช็คให้ซ่อน/ปิดใช้งานในโหมดดูอย่างเดียวด้วยเสมอ (รูปแบบ pattern จาก PR #181: loop ซ่อนปุ่มที่มี id ระบุไว้)
5. **`created_at` ของร่าง (draft) ต้องระบุเป็นเวลาไทยเองจากฝั่งแอป** ไม่พึ่งเวลา default ของ DB server (PR #169)

## ประวัติการเปลี่ยนแปลง (PR #1 – #185)

เรียงจากเก่าไปใหม่ กลุ่มตามธีม สรุปย่อ — อ่านรายละเอียดเต็มได้จาก PR ใน GitHub

### รากฐานระบบ (early history) — สร้างโครงระบบตั้งแต่เริ่ม (#1–#83)
- #1–#4 ตั้งฐานระบบ: แก้ compatibility MySQL, ระบบ ID แบบ UUID, ลบชื่อ Supabase ที่ค้างจากต้นแบบเดิม, แก้บั๊ก ENUM ใบอนุญาตพังตอน import Excel
- ในช่วงเดียวกันนี้ยังมีงานที่ทำคู่ขนานกัน 2 เซสชัน (บันทึกไว้เพื่อไม่ให้สับสนถ้าเจอโค้ด/comment ที่อ้างถึง):
  แก้บั๊กแปลงวันที่ พ.ศ./ISO ตอนเพิ่ม/แก้ใบอนุญาตในหน้าแอดมิน (`saveLicense()` เคยส่งวันที่ พ.ศ. ดิบ
  ไปที่ backend ตรงๆ ทำให้ MySQL error), สร้างตาราง `operator_accounts` + ฟีเจอร์ import อีเมลจาก
  Excel เพื่อ override อีเมลรับ OTP ของผู้ประกอบการ, สร้าง `frontend/js/admin-utils.js` ขึ้นใหม่
  (เคยหายไปจาก repo ระหว่างทาง), และแก้บั๊ก 403 ที่หน้าแอดมินจากการเลือก JWT token ผิดฝั่งใน
  `api-client.js` (ปัจจุบันแก้ให้เลือก token ตาม path ของหน้าที่เปิดอยู่แล้ว)
- #5, #8, #12–#13, #24 ระบบ audit log: เพิ่ม/revert endpoint, ใส่ timezone กรุงเทพฯ + label ให้ log อ่านง่าย, แก้ log ซ้ำซ้อน/ไม่มีรายละเอียดตอนลบ-แก้-import, ซ่อน log แบบ bulk เก่า, ปรับให้อ่านง่ายขึ้น (วันที่ไทย, ชื่อไฟล์ import/export)
- #9–#11 หน้าดูใบยื่นแบบของแอดมิน (เวอร์ชันแรก) + revert เพราะมีปัญหา แล้วแก้ใหม่ให้ไปมาระหว่าง step ได้อิสระในโหมดดูอย่างเดียว
- #14–#18 โหมดดูอย่างเดียว: เก็บ/แสดงค่ารายได้รวมตามงบการเงิน, อนุญาตส่งได้แม้ไม่แนบเอกสารบังคับ (ภายหลังกลับมาบังคับใหม่ที่ #156), แก้สถานะผิด/ย่อ banner, เก็บฟิลด์ "สถานี/ช่องรายการ" ของใบอนุญาต, แก้ลำดับแถวใบอนุญาตสลับกันเองระหว่าง save
- #19–#20 ปรับ scrollbar/padding sidebar
- #21–#26 หน้า export รายงาน: ผูก filter ปีย้อนหลัง/สถานะชำระเงิน, แก้ปี พ.ศ. แปลงซ้ำสอง, ดึงรายงานจากข้อมูลที่ยื่นจริงแทนข้อมูล master ที่ import ไว้, ตั้งชื่อไฟล์ export ตามวันที่ export, เติมคอลัมน์การเงินในรายงานผู้รับใบอนุญาต
- #27–#32 ปรับ UI ตารางแอดมินให้เป็นมาตรฐานเดียวกัน (border/padding/font), จัดกึ่งกลางคอลัมน์ต่างๆ, ปรับ dashboard card
- #33–#41 ฟีเจอร์จัดการบัญชีผู้ประกอบการ: แก้อีเมลได้จริง, ค้นหาแบบ real-time, ปรับไอคอน sidebar, เอา filter export ที่ใช้งานไม่ได้ออก (revert แล้วตัดเฉพาะบางส่วนที่ #38), จัดหน้า audit log filter/label ใหม่
- #42 ย่อคอมเมนต์ที่ยาวเกินไปทั้ง backend/frontend
- #43–#46 เปลี่ยนไอคอน sidebar เป็น FontAwesome ทั้งหมด
- #47–#55 หน้า login: เพิ่ม/ปรับ tooltip อธิบาย OTP หลายรอบ (tooltip → modal → กลับมาเป็น tooltip), ออกแบบช่องกรอก OTP ใหม่เป็น 6 กล่องแยกพร้อม auto-advance, ปรับอีเมล OTP (เอารหัสออกจากหัวเรื่อง, เปลี่ยนชื่อผู้ส่ง)
- #56–#58 แก้บั๊กรายงานใบอนุญาต, เพิ่มปุ่มลบผู้ประกอบการ, ลบข้อมูลตัวอย่าง (dummy seed) ออกจาก schema.sql
- #59 เพิ่มระบบสิทธิ์แอดมิน 2 ระดับ (super_admin, admin)
- #60–#78 ปรับปรุงหน้าจัดการแอดมิน/โปรไฟล์ต่อเนื่องหลาย PR: สลับคอลัมน์ตาราง, เพิ่มหน้าโปรไฟล์, แก้ autofill/ค้างค่าค้นหาตอนรีเฟรช, การ์ดโปรไฟล์ sidebar (ธีมสี, ชื่อ-อีเมล-ปุ่มออกจากระบบ), แปล role เป็นไทย, ปุ่มโชว์/ซ่อนรหัสผ่าน, แก้ script tag ซ้ำทำ console error
- #79–#83 ระบบนำเข้าข้อมูล (import): เพิ่ม rollback, รองรับหลายอีเมลในเซลล์เดียว, อนุญาตอีเมลว่าง, import แถวที่อ่านได้ทั้งหมดแทนที่จะข้าม, แสดงทุกแถวในตาราง preview แทนการจำกัดจำนวน

### ข้อมูลผู้เสียภาษี / ใบอนุญาต / นำเข้าข้อมูล (import)
- #84 จำกัดเมนูนำเข้าข้อมูลให้ `super_admin` เท่านั้น
- #85–#87 แก้ตาราง preview นำเข้าข้อมูล (วันที่หาย, แสดงสถานะใบอนุญาตเป็นไทย, ใช้ "เลขผู้เสียภาษี" แทน "Tax ID")
- #88 เพิ่มปุ่มสร้างบัญชีผู้ประกอบการทีละคน
- #105–#109, #115, #117–#125 ปรับโครงสร้าง/label ฟิลด์ผู้เสียภาษี-ใบอนุญาต (ref_no, sub_type/round_type, ประเภท, รอบ, สถานะ) หลายรอบ — จบที่ #125 (ประเภทเป็น dropdown, ref_no label เป็นทางการ)
- #103, #110–#112, #124 แก้บั๊กเกี่ยวกับใบอนุญาตหาย/ตัวเลือก dropdown ผิด/ค่าไม่อัปเดตหลัง import

### ใบนำส่งเงิน (bill payment / deposit slip) — QR, บาร์โค้ด, ดีไซน์
- #89–#101, #139–#144 ชุด PR ปรับรูปแบบใบนำส่งเงินให้ตรง spec ธนาคาร: QR ให้เป็น Thai QR Payment (EMVCo TLV+CRC) จริง, บาร์โค้ด/QR ไม่เบลอตอนพิมพ์, ฟอนต์/น้ำหนักตัวอักษรตรงตามแบบอ้างอิง, ขนาดกระดาษ A4, จัดตำแหน่งข้อความแจ้งกำหนดชำระ

### Schema / Migration / โครงสร้างฐานข้อมูล
- #102 เพิ่ม mock integration layer เชื่อม Data Center/SAP(ZAT)
- #104 แก้ migration 011 (`system` เป็นคำสงวนใน MySQL 8)
- #106, #108, #122, #128–#129 ลบคอลัมน์/migration ที่ไม่ได้ใช้, รวม migration ซ้ำซ้อน, ย่อ COMMENT ให้สั้นลง

### CI/Test
- #113 เพิ่ม automated backend test suite + CI (เจอบั๊กจริง 1 ตัวจากการเขียนเทสต์)
- #116 เอา test suite + CI ออกทั้งหมด (นโยบายเปลี่ยน — ปัจจุบันไม่มีเทสต์อัตโนมัติ)

### ฟอร์มยื่นแบบ (Step 1–5) — คำนวณ/validation/ปีบัญชี
- #111, #131–#133 แก้ปีบัญชี (fiscal year) เริ่มต้นวิ่งไม่หยุด, ข้อมูลปีเก่าค้าง, ร่างหายตอนสลับปี, กรองใบอนุญาตตามช่วงบัญชีไม่ตรง
- #134–#136 แก้ TypeError หน้าสรุป Step 5 กรณีเลยกำหนด, แก้เบี้ยปรับคำนวณผิดพารามิเตอร์, จัดรูปแบบตัวเลขใน ชส.01
- #137 ล้างค่ารายได้ตามงบการเงินเมื่อสลับปีบัญชี
- #151–#154 โหมดดูอย่างเดียว: ล็อกช่องลดหย่อน/แถวรายได้กำหนดเอง, คืน/สลับหน้าตากล่องสรุปรายได้ให้ตรง spec

### สถานะใบยื่นแบบ & สิทธิ์แอดมิน
- #138, #145–#150, #161, #164–#168 วน iterate เรื่องแอดมินแก้ไขข้อมูลได้/ไม่ได้ — เริ่มจากให้แก้ได้ (#138) → เอาปุ่มบันทึกรับชำระออก (#145) → เพิ่มให้ตีกลับได้ (#146–148, revert ที่ #149, เพิ่มใหม่ที่ #161) → **จบที่ #168: เอาความสามารถแก้ไขข้อมูลของแอดมินออกทั้งหมด** (สถานะปัจจุบัน: แอดมินดูอย่างเดียว + ตีกลับได้เท่านั้น)
- #159, #162–#163, #171 เอาปุ่ม/endpoint จำลอง (mock) ออก, เปลี่ยน `prompt()/confirm()` เป็น modal สำหรับเหตุผลตีกลับ
- #165–#166 ซ่อนปุ่ม "ดูข้อมูล" และบล็อก backend ไม่ให้แอดมินเปิดดูใบยื่นแบบสถานะ `draft`

### บันทึกร่าง (Save Draft) — ฟีเจอร์ใหญ่ที่ทำหลาย PR ต่อเนื่อง
- #157 เพิ่มระบบบันทึกร่างแบบกดเอง + ตารางร่างหน้าแรก
- #158–#160, #169, #176 ปรับ UI รายการร่าง (จากตารางเป็นข้อความสั้น), แก้บั๊กปุ่มชนกับ auto-save เดิม (สร้าง draft ซ้ำ — บั๊กร้ายแรง #159), แก้เวลาบันทึกคลาดเคลื่อน, ไม่โชว์ยอดเงินให้ร่างที่กรอกไม่ครบ
- #173–#175 ทำให้ไฟล์แนบ (Step 6) ถูกเก็บไปกับร่างด้วย ไม่หายเมื่อบันทึกร่าง/กด "ทำต่อ" (แก้บั๊ก doc_type ถูกตัดทำให้ไฟล์ชื่อยาวหาย)
- #172 ลบไฟล์แนบจริงบนดิสก์เมื่อลบใบยื่นแบบ กันไฟล์กำพร้า

### เอกสารแนบ (Step 6) & โหมดดูอย่างเดียว — งานล่าสุดในเซสชันนี้
- #155–#156 เปลี่ยนปุ่มปิดแบนเนอร์เป็น "กลับหน้าหลัก", บังคับแนบเอกสารหลัก 3 อันก่อนยืนยันนำส่ง
- #170, #178 ปุ่ม "ดูเอกสาร" เปิด preview แท็บใหม่แทนดาวน์โหลด, ซ่อนปุ่ม "แนบไฟล์" เมื่อมีไฟล์แนบอยู่แล้ว
- #177 เปลี่ยนชื่อระบบจาก e-BCS → NBTC Filing ทุกจุด
- #179–#182 ปุ่ม/ลิงก์ "หน้าหลัก" ในหน้ากรอกแบบและโหมดดูอย่างเดียว — เพิ่ม, ซ่อนโหมดดูอย่างเดียว, แก้กลับ (สลับจุดที่ต้องซ่อนกับต้องโชว์)
- #183–#184 เพิ่มดอกจันกำกับเอกสารแนบบังคับ 3 รายการ (งบดุลการเงิน/ชส.01/ชส.02) → เจอบั๊กเอกสารที่แนบไว้แล้วหายไปเพราะดอกจันไปปนกับ `doc_type` matching (ดูข้อ 1 ในหัวข้อ "จุดที่ควรระวัง" ด้านบน) → แก้ใน #184
- #185 เพิ่ม README.md และ CLAUDE.md (ไฟล์นี้) สรุปโปรเจกต์และรวมประวัติ PR ทั้งหมด

## สถานะปัจจุบัน (ล่าสุด ณ PR #185)

- ระบบใช้ชื่อ "NBTC Filing" ทุกจุด (ไม่มี "e-BCS" เหลือในหน้าจอแล้ว)
- ใบยื่นแบบที่ไม่ใช่ draft: ดูอย่างเดียว + พิมพ์เอกสารได้ ทั้งฝั่งผู้ประกอบการและแอดมิน แอดมินแก้ไขข้อมูลไม่ได้ ตีกลับเป็นร่างได้อย่างเดียว (พร้อม modal ใส่เหตุผล)
- บันทึกร่างเก็บทั้งข้อมูลฟอร์มและไฟล์แนบ ไม่หายเมื่อกลับมาทำต่อ
- เอกสารแนบบังคับ 3 รายการ (งบดุลการเงิน, ชส.01, ชส.02) มีดอกจันกำกับ และเช็คครบก่อนยืนยันนำส่งได้
- ไม่มี automated test — ทดสอบด้วยมือทุกครั้งก่อน merge

## Known Issues / งานค้าง

รายการนี้เป็นเรื่องที่ยังไม่ได้แก้ ณ ตอนที่เขียน รอข้อมูล/ระบบจากภายนอก ไม่ใช่บั๊กที่แก้ในโค้ดได้เอง

1. **Data Center/SAP(ZAT) integration ยังเป็น mock ทั้งหมด** — `backend/app/services/integration_service.py`
   ตั้ง `INTEGRATION_MODE=mock` เป็นค่าเริ่มต้น และ return ค่าอ้างอิงปลอม (`MOCK-DC-...`,
   `MOCK-SAP-...`) ยังไม่มี API spec จริงจากหน่วยงาน และ**ไม่มี webhook รับแจ้งชำระเงินอัตโนมัติเลย**
   — หมายความว่าตอนนี้ไม่มีทางมาร์คสถานะ "ชำระแล้ว" (`paid`) โดยอัตโนมัติจากธนาคาร ต้องรอ spec จริง
   ก่อนถึงจะเชื่อมต่อได้
2. **Biller ID ของ QR/บาร์โค้ดยังไม่ยืนยันว่าลงทะเบียนกับธนาคารจริงหรือยัง** — QR ที่พิมพ์ออกมาเป็น
   Thai QR Payment (EMVCo) มาตรฐานถูกต้องแล้ว (PR #141) แต่การจ่ายเงินจริงผ่าน QR ยังทำไม่ได้
   จนกว่าจะยืนยันเรื่องนี้กับธนาคาร/กสทช.
3. **ระบบให้ผู้ประกอบการเปลี่ยนอีเมลของตัวเองยังไม่มี** — ปัจจุบันมีแค่แอดมินแก้อีเมลผู้เสียภาษีให้ได้
   (`backend/app/routes/admin.py` ส่วนจัดการ taxpayer) แนวทางที่ตกลงไว้คือ "ผู้ประกอบการขอเปลี่ยน
   → แอดมินอนุมัติ" แต่ยังไม่เริ่มพัฒนา

