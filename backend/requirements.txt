
# Python Libraries ที่ใช้ในโปรเจกต์
# ล็อค version เพื่อให้ทุกเครื่อง build ได้เหมือนกัน


# Web framework หลัก
Flask==3.0.3

# อนุญาตให้ frontend เรียก API ข้าม domain ได้ (CORS)
flask-cors==4.0.1

# รัน Flask แบบ production (แทน flask run)
gunicorn==22.0.0

# ระบบ JWT Token (Login)
Flask-JWT-Extended==4.6.0

# เข้ารหัส password
bcrypt==4.1.3

# เชื่อมต่อ MySQL
PyMySQL==1.1.1

# PyMySQL ต้องการ cryptography
cryptography==42.0.8

# เชื่อมต่อ Redis (OTP)
redis==5.0.7

# อ่าน/เขียน Excel (Import + Export)
openpyxl==3.1.4

Flask-Mail==0.10.0